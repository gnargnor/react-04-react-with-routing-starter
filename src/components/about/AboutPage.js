import React from 'react';
import { Link } from 'react-router-dom';

class AboutPage extends React.Component {
    render() {
        return (
            <div>
                <h1>About</h1>
                <p>Check the package.json for included libraries</p>
            </div>
        );
    }
}

export default AboutPage;